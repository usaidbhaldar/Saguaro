@model System.Collections.Generic.IEnumerable<Saguaro.Logging.TableEntities.LogByIPAddressTableEntity>

@{
    Layout = null;
    //Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>IP</title>
</head>
<body>
    <div>
        @foreach(var log in Model)
        {            
            <h3>@log.IPAddress</h3>
            <div>@log.UserName</div>
            <div>@log.Email</div>
            <div>@TimeZoneInfo.ConvertTimeFromUtc(log.Timestamp, TimeZoneInfo.FindSystemTimeZoneById("Pacific Standard Time"))</div>
            <div>@log.ActivityType</div>

            <div>@log.Company</div>

            <hr />
        }
    </div>
</body>
</html>

